import { PfMaskZipcodeDirective } from './pf-mask-zipcode.directive';

describe('PfMaskZipcodeDirective', () => {
  it('should create an instance', () => {
    const directive = new PfMaskZipcodeDirective();
    expect(directive).toBeTruthy();
  });
});
